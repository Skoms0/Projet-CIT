apiVersion: sparkoperator.k8s.io/v1beta2
kind: SparkApplication
metadata:
  name: kafka-consumer
  namespace: default
spec:
  type: Python
  mode: cluster
  pythonVersion: "3"
  image: spark-master:latest
  imagePullPolicy: IfNotPresent
  mainApplicationFile: local:///opt/spark-apps/kafka_consumer.py
  sparkVersion: "3.5.0"
  restartPolicy:
    type: OnFailure
    onFailureRetries: 3
  driver:
    cores: 1
    memory: 1g
    serviceAccount: spark
  deps:
    jars:
      - local:///opt/spark/jars/spark-sql-kafka-0-10_2.12-3.5.0.jar

  executor:
    cores: 1
    instances: 2
    memory: 1g

